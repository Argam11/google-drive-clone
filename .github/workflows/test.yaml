name: Test project

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    container: node:20
  steps:
    - uses: actions/checkout@v4
      with:
        node-version: 20
    - run: yarn lint
    - run: yarn build
    - run: yarn test